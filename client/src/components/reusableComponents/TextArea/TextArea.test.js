import React from "react";
import { render } from "react-testing-library";

import TextArea from "../TextArea";

describe("<TextArea />", () => {
  it("renders TextArea", () => {
    render(<TextArea />);
  });
});
